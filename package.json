{
  "name": "inno-template",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": { "hono": "^4", "jose": "^5", "@cloudflare/containers": "^0.2.0" },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.12.21", "@cloudflare/workers-types": "^4",
    "typescript": "^5", "vitest": "~3.0", "wrangler": "4.72.0"
  }
}
