# Container-only files, deleted when an mcp-type repo is pruned.
# One path per line; entries ending "/" delete the whole directory.
# (app/ is re-added by this overlay: index.ts, package.json, and the .needs-build marker.)
Dockerfile
.dockerignore
app/
lib/
.dev.vars.example
