# Container-only files, deleted when a function-type repo is pruned.
# One path per line; entries ending "/" delete the whole directory.
# (app/ is re-added by this overlay: index.ts + the .needs-build marker.)
Dockerfile
.dockerignore
app/
lib/
