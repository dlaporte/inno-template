# Files deleted when an mcp-container-type repo is pruned.
# One path per line; entries ending "/" delete the whole directory.
# (This overlay keeps the container shape: Dockerfile, .dockerignore, lib/,
# app/storage.py and app/.needs-build stay as they are, and the files beside
# this manifest replace their root namesakes. The manifest must exist even
# when the list is short; the starter is MCP-only, so the HTML templates go.)
app/templates/
